## Что такое Dapper
- **Micro-ORM** — минимальная обёртка над ADO.NET на 1 файл.
- Расширение для `IDbConnection` — методы `Query`, `Execute`, `QueryMultiple`.
- Максимальная производительность — почти как raw ADO.NET.
